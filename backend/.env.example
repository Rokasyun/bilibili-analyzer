PORT=3001
CORS_ORIGIN=http://localhost:5173

# B站Cookie（可选，用于避免API限流）
# 获取方式：登录B站后，在浏览器开发者工具中复制Cookie
# BILIBILI_COOKIE=your_cookie_here

# 代理配置（可选，用于绕过IP限流）
# HTTP_PROXY=http://proxy-server:port
# HTTPS_PROXY=http://proxy-server:port
